import @java Queue java.util.ArrayDeque
import @java Objects java.util.Objects

fn identity(obj: java.lang.Object) -> java.lang.Object = {
    obj
}

fn filterNotEquals(list: java.util.List, obj: java.lang.Object) -> java.util.List = {
    var queue = new Queue
    queue.addAll(list)
    var result = new java.util.ArrayList
    while !queue.isEmpty() {
        var head = queue.poll()
        if !Objects.equals(head, obj) {
            result.add(head)
        }
    }
    result
}
